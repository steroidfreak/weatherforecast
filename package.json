{
  "name": "kanban-mcp-server",
  "version": "1.0.0",
  "type": "module",
  "description": "MCP server for Kanban board widget",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build:web": "cd web && npm run build",
    "dev": "npm run build:web && npm start"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "express": "^4.18.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}
